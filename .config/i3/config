# ╔═══════════════════════════════════════════╗
# ║        i3 — Palette Noctavia              ║
# ║    Tokyo Night × Catppuccin Mocha         ║
# ╚═══════════════════════════════════════════╝

# ── Mod key ──────────────────────────────────
set $mod Mod4

# ── Couleurs Noctavia ─────────────────────────
set $base    #1a1b2e
set $mantle  #1e1e2e
set $surface #24253e
set $overlay #2e2f52
set $purple  #bb9af7
set $mauve   #cba6f7
set $cyan    #7dcfff
set $teal    #89dceb
set $pink    #f38ba8
set $peach   #fab387
set $text    #cdd6f4
set $subtext #a9b1d6
set $muted   #565f89
set $border  #bb9af733

# ── Police ───────────────────────────────────
font pango:JetBrainsMono Nerd Font 10

# ── Apparence des fenêtres ───────────────────
# class                 border    bg        text     indicator child_border
client.focused          $purple   $surface  $text    $mauve    $purple
client.focused_inactive $overlay  $mantle   $muted   $overlay  $overlay
client.unfocused        $mantle   $base     $muted   $mantle   $mantle
client.urgent           $pink     $base     $pink    $pink     $pink
client.placeholder      $base     $base     $text    $base     $base
client.background       $base

# ── Bordures ─────────────────────────────────
default_border pixel 2
default_floating_border pixel 2
hide_edge_borders smart
gaps inner 8
gaps outer 4

# ── Démarrage ────────────────────────────────
exec --no-startup-id dex --autostart --environment i3
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
exec --no-startup-id nm-applet
#exec --no-startup-id picom --config ~/.config/picom/picom.conf
exec_always --no-startup-id ~/.config/polybar/launch.sh
exec --no-startup-id feh --bg-fill ~/Pictures/wallpapers/noctavia.png

# ── Volume (pactl) ───────────────────────────
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym XF86AudioMute        exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute     exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# ── Raccourcis principaux ────────────────────
bindsym $mod+Return       exec kitty
bindsym $mod+d            exec rofi -show drun -show-icons -theme ~/.config/rofi/noctavia.rasi
bindsym $mod+shift+d      exec rofi -show run  -theme ~/.config/rofi/noctavia.rasi
bindsym $mod+q            kill
bindsym $mod+Shift+q      kill
bindsym $mod+Shift+e      exec i3-nagbar -t warning -m 'Quitter i3 ?' -B 'Oui' 'i3-msg exit'
bindsym $mod+Shift+r      restart
bindsym $mod+Shift+c      reload
bindsym Print exec --no-startup-id scrot ~/Pictures/screenshots/%Y-%m-%d_%H-%M-%S.png
# ── Déplacement du focus ─────────────────────
bindsym $mod+h            focus left
bindsym $mod+j            focus down
bindsym $mod+k            focus up
bindsym $mod+l            focus right
bindsym $mod+Left         focus left
bindsym $mod+Down         focus down
bindsym $mod+Up           focus up
bindsym $mod+Right        focus right

# ── Déplacement des fenêtres ─────────────────
bindsym $mod+Shift+h      move left
bindsym $mod+Shift+j      move down
bindsym $mod+Shift+k      move up
bindsym $mod+Shift+l      move right
bindsym $mod+Shift+Left   move left
bindsym $mod+Shift+Down   move down
bindsym $mod+Shift+Up     move up
bindsym $mod+Shift+Right  move right

# ── Layout ───────────────────────────────────
bindsym $mod+b            splith
bindsym $mod+v            splitv
bindsym $mod+s            layout stacking
bindsym $mod+w            layout tabbed
bindsym $mod+e            layout toggle split
bindsym $mod+f            fullscreen toggle
bindsym $mod+Shift+space  floating toggle
bindsym $mod+space        focus mode_toggle
bindsym $mod+a            focus parent

# ── Redimensionnement ────────────────────────
mode "resize" {
  bindsym h resize shrink width  10 px or 10 ppt
  bindsym j resize grow   height 10 px or 10 ppt
  bindsym k resize shrink height 10 px or 10 ppt
  bindsym l resize grow   width  10 px or 10 ppt
  bindsym Left  resize shrink width  10 px or 10 ppt
  bindsym Down  resize grow   height 10 px or 10 ppt
  bindsym Up    resize shrink height 10 px or 10 ppt
  bindsym Right resize grow   width  10 px or 10 ppt
  bindsym Return mode "default"
  bindsym Escape mode "default"
  bindsym $mod+r mode "default"
}
bindsym $mod+r mode "resize"

# ── Workspaces ───────────────────────────────
set $ws1  "1"
set $ws2  "2"
set $ws3  "3"
set $ws4  "4"
set $ws5  "5"
set $ws6  "6"
set $ws7  "7"
set $ws8  "8"
set $ws9  "9"
set $ws10 "10"

bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# ── Scratchpad ───────────────────────────────
bindsym $mod+minus       move scratchpad
bindsym $mod+Shift+minus scratchpad show

# ── Barre i3bar ──────────────────────────────
##bar {
#  status_command i3status --config ~/.config/i3/i3status.conf
#  position top
#  font pango:JetBrainsMono Nerd Font 10
#
#  colors {
#    background         $base
#    statusline         $text
#    separator          $muted
#    #                  border    bg        text
#    focused_workspace  $purple   $surface  $text
#    active_workspace   $overlay  $mantle   $subtext
#    inactive_workspace $base     $base     $muted
#    urgent_workspace   $pink     $base     $pink
#    binding_mode       $peach    $base     $peach
#  }
#}
